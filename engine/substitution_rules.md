## FORMAT
primary_id -> [alt_id_1, alt_id_2]

## RULES
- Keep same pattern and cns
- Prefer same laterality
- If no valid substitute exists → return UNSATISFIABLE_CONSTRAINTS

## EXAMPLE
trap_bar_deadlift -> [barbell_deadlift, db_rdl]
pullups -> [inverted_row]