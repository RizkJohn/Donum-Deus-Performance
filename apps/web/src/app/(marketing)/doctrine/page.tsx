import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The Doctrine",
  description:
    "The full Deus Performance methodology. The priority hierarchy, the constraint logic, the reason the engine works the way it does.",
};

export default function DoctrinePage() {
  return (
    <main className="min-h-screen">
      {/* TODO: Doctrine page — expand priority hierarchy into institutional prose */}
    </main>
  );
}
