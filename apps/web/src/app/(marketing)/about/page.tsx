import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The Coach",
  description: "About the coach behind Deus Performance and Riz Management LLC.",
};

export default function AboutPage() {
  return (
    <main className="min-h-screen">
      {/* TODO: About page — The Coach */}
    </main>
  );
}
